## 2025-05-14 - [CSS Cursor Property Syntax]
**Learning:** The CSS `cursor` property value for a disabled state is `not-allowed` (kebab-case), not `not_allowed` (snake-case). Using the incorrect syntax results in the property being ignored by the browser.
**Action:** Always use `not-allowed` for disabled cursors and double-check CSS property value syntax during implementation.
